====== Void Linux ======

===== Pros =====

  *Rolling release.
  *Native non-systemd init system ([[:void:runit|runit]]).
  *Independent distribution.
  *Text mode installation.
  *Binary based.
  *Package manager ([[:void:xbps|XBPS]]).
  *Wide processor architecture support (amd64/x86_64, i686/x86, aarch64, armv7, armv6).
  *Stable.
  *Separates free and non-free software.
  *Multiple C libraries support (glibc, musl).

===== Cons =====

  *Small community & amount of packages.
  *Multiple kernel versions (not only 1 kernel name for each kernel type).